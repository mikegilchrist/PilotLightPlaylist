# Date: 2025-09-17
# Version: 1.0.0
# Purpose: Example configuration file for PilotLight playlist updater
# Usage: Copy this file to `config.yaml` and fill in the correct playlist IDs
# Chat name: PilotLight Playlist Config Example
# GPT Model: OpenAI o4-mini
# Attribution: Generated by Michael Gilchrist in collaboration with ChatGPT

# NOTE: This file is only an example. It should be renamed to `config.yaml` for actual use.
#       Keep `config.yaml` out of version control (e.g., add it to your `.gitignore`).

playlists:
  upcoming:
    id: "5X00xxxxxxxxxxxxxxxxxxxxxxxx"
    name: "Upcoming Shows"
  recent:
    id: "4M9Cxxxxxxxxxxxxxxxxxxxxxxxx"
    name: "Recent Shows"
